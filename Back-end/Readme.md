## Setup Instructions

1. Clone the repository
2. Run: npm install
3. Create a file named config.env inside the config folder
4. Where you have tp provide the some information like PORT : FRONTEND_URL : MONGO_URI :
5. Add your own values
6. Run: npm run dev 